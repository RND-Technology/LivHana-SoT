### 12.2 Environment Issues
```bash
# Check environment variables
env | grep NODE
env | grep GCP

# Reload environment
source .env

# Check shell configuration
echo $SHELL
cat ~/.zshrc | grep -i node
```
