{
  "name": "livhana-sot",
  "version": "1.0.0",
  "description": "Liv Hana Source of Truth - Tier 1 Services",
  "scripts": {
    "build": "tsc --build",
    "build:watch": "tsc --build --watch",
    "lint": "eslint . --ext .ts,.js",
    "lint:fix": "eslint . --ext .ts,.js --fix",
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "test:watch": "playwright test --watch",
    "dev": "concurrently \"npm run build:watch\" \"npm run test:watch\"",
    "optimize": "npm run lint:fix && npm run build && npm run test",
    "tier1": "npm run optimize && echo '✅ Tier 1 Option A optimization complete'",
    "performance": "npm run build && npm run test && echo '🚀 Performance check complete'"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "@typescript-eslint/eslint-plugin": "^6.0.0",
    "@typescript-eslint/parser": "^6.0.0",
    "eslint": "^8.0.0",
    "typescript": "^5.0.0",
    "concurrently": "^8.0.0",
    "nodemon": "^3.0.0"
  },
  "dependencies": {
    "express": "^5.1.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}
